# AgentX 安装指南

## macOS 安装说明

由于 AgentX 是开源项目，未使用付费的 Apple Developer 证书进行签名，首次打开时会被 macOS Gatekeeper 阻止。这是 macOS 的安全机制，**完全正常**。

### 🚀 快速安装（推荐）

下载并解压 DMG 后，在终端中运行：

```bash
cd /Applications
xattr -cr AgentX.app
open AgentX.app
```

这会移除 Gatekeeper 的隔离属性，允许应用直接运行。

---

### 📋 手动安装方法

#### 方法 1：右键打开（最简单）

1. 双击下载的 `.dmg` 文件
2. 将 `AgentX.app` 拖到 `Applications` 文件夹
3. 在 `Applications` 文件夹中，**右键点击** `AgentX.app`
4. 选择 **"打开"**（不是双击）
5. 在弹出的对话框中点击 **"打开"**

✅ **一次授权后，以后可以正常双击打开**

#### 方法 2：系统设置中授权

1. 双击 `AgentX.app`（会被阻止）
2. 打开 **"系统设置"** > **"隐私与安全性"**
3. 向下滚动，找到提示 `"AgentX" was blocked from use because it is not from an identified developer`
4. 点击 **"仍要打开"** 按钮
5. 在确认对话框中再次点击 **"打开"**

#### 方法 3：使用安装脚本（最快）

我们提供了一个安装脚本来自动化这个过程：

```bash
# 1. 挂载 DMG
open agentx-*.dmg

# 2. 运行安装脚本（在 DMG 中）
./install.sh
```

脚本会自动：
- 复制应用到 Applications 文件夹
- 移除 Gatekeeper 隔离属性
- 启动应用

---

### ⚠️ 为什么会被阻止？

macOS Gatekeeper 会检查应用的签名：
- ✅ **App Store 应用**：由 Apple 审核和签名
- ✅ **已公证的应用**：使用付费 Developer ID 签名并经 Apple 公证
- ⚠️ **未签名或 ad-hoc 签名**：需要用户手动授权

AgentX 是开源项目，使用 ad-hoc 签名（免费），因此需要手动授权。

### 🔒 安全说明

- AgentX 是**完全开源**的项目，源代码可在 [GitHub](https://github.com/sxhxliang/agent-studio) 查看
- 所有发布版本在 [GitHub Releases](https://github.com/sxhxliang/agent-studio/releases) 公开构建
- 你可以选择从源代码自行编译：`cargo build --release`

### ❓ 常见问题

**Q: 每次更新都需要重新授权吗？**
A: 是的，每次下载新版本都需要重新授权一次。这是 macOS 对未签名应用的限制。

**Q: 为什么不使用正式签名？**
A: Apple Developer 证书需要支付 $99/年。作为开源项目，我们选择免费分发。如果您希望支持正式签名，欢迎赞助项目。

**Q: 这样做安全吗？**
A: 是的。您可以：
1. 查看源代码确认安全性
2. 检查 GitHub Actions 构建日志
3. 自行从源码编译

**Q: 能否获得自动更新？**
A: 应用内置了更新检查功能，但下载新版本后仍需手动授权一次。

---

## Windows 安装说明

### MSI 安装包（推荐）

1. 双击 `agentx-*-x86_64-windows.msi`
2. 按照安装向导完成安装
3. 从开始菜单或桌面快捷方式启动

### NSIS 安装程序

1. 双击 `agentx-*-x86_64-windows-setup.exe`
2. 按照安装向导完成安装
3. 选择安装位置和快捷方式选项

### 便携版

1. 下载 `agentx-*-x86_64-windows.exe`
2. 将文件放到任意文件夹
3. 双击运行（无需安装）

### Windows Defender SmartScreen

首次运行可能会显示 SmartScreen 警告：
1. 点击 **"更多信息"**
2. 点击 **"仍要运行"**

这是因为应用未经微软签名（需要付费证书）。

---

## Linux 安装说明

### Debian/Ubuntu（.deb 包）

```bash
sudo dpkg -i agentx-*-x86_64-linux.deb
sudo apt-get install -f  # 安装依赖
agentx
```

### AppImage（通用）

```bash
chmod +x agentx-*-x86_64-linux.AppImage
./agentx-*-x86_64-linux.AppImage
```

AppImage 无需安装，可以直接运行。

### Tarball（手动安装）

```bash
tar -xzf agentx-*-x86_64-linux.tar.gz
cd agentx-*-x86_64-linux
./agentx
```

可以将二进制文件移动到 `/usr/local/bin` 以便全局访问：

```bash
sudo mv agentx /usr/local/bin/
```

---

## 从源代码编译

如果你想完全掌控构建过程，可以自行编译：

### 前置要求

- Rust 1.88+ (`rustup install stable`)
- 对应平台的系统依赖（见 `.github/workflows/build.yml`）

### 编译步骤

```bash
# 克隆仓库
git clone https://github.com/sxhxliang/agent-studio.git
cd agent-studio

# 编译
cargo build --release

# 运行
./target/release/agentx
```

### 创建安装包

```bash
# 安装 cargo-packager
cargo install cargo-packager --locked

# 打包（根据你的平台）
cargo packager --release --formats dmg      # macOS
cargo packager --release --formats nsis,wix # Windows
cargo packager --release --formats deb      # Linux
```

---

## 需要帮助？

- 📖 查看 [README.md](README.md) 了解功能介绍
- 🐛 遇到问题？提交 [Issue](https://github.com/sxhxliang/agent-studio/issues)
- 💬 加入讨论：[Discussions](https://github.com/sxhxliang/agent-studio/discussions)

---

**祝使用愉快！🎉**
